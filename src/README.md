# Source layout

Production code is added here by the approved implementation tickets: `core/`, `android/`, `ios/`, and supporting `organizer-tools/` and `share-site/` when their tickets are ready. No application implementation exists yet. See [the active plan](../docs/ticketboard/implementation-plan.md) and [repository rules](../AGENTS.md).
